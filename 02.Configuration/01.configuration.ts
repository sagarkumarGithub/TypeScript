let userName : string = "Max";
console.log(userName);

// run command 
//-------------

// tsc --init (to create tsconfig.json file )
// tsc (to compile the file ) 
// tsc --watch (to watch the file any time it changes)
// npm install @types/node (to install node types if i am using node modules)

