// userName is a string variable
let userName : string;
userName = "Maximilian";
console.log(userName);

// age is a number variable
let age : number;
age = 30;
console.log(age);

// isAdult is a boolean variable
let isAdult : boolean;
isAdult = true;
console.log(isAdult);

// ---------------------------------

// where we need to define the type
function addTwoNumbers(num1: number, num2: number) {
    return num1 + num2;
}
console.log(addTwoNumbers(2, 2));





